name: CI - Echo Build Message

on:
  push:
    branches:
      - '**'
  pull_request:

jobs:
  echo-build:
    runs-on: ubuntu-latest
    steps:
      - name: Show build message
        run: |
          echo "building..."
